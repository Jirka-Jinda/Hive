# Workspace
node_modules/
dist/

# Cloned / managed repos — never committed to the workspace repo
repos/

# Backend data (SQLite DB, encrypted credentials, cloned repos)
packages/backend/data/
**/data/*.db
**/data/*.db-shm
**/data/*.db-wal

# Electron build output
packages/electron/release/

# Environment
.env

# OS
.DS_Store
Thumbs.db
